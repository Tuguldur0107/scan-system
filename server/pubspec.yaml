name: server
description: Scan System API server
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  shared:
    path: ../packages/shared
  postgres: ^3.1.1
  dart_jsonwebtoken: ^2.12.0
  bcrypt: ^1.1.3
  crypto: ^3.0.3
  uuid: ^4.2.1
  dotenv: ^4.2.0

dev_dependencies:
  dart_frog_gen: ^1.1.0
  build_runner: ^2.4.6
  test: ^1.24.0
